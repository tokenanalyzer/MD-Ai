# core/security

Provider key vault (envelope encryption, `provider_credentials`), log
redaction middleware, Guardian Agent policy checks, audit log writer. See
`docs/architecture/07-security-model.md`.
