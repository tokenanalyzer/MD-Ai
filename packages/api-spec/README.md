# packages/api-spec

`openapi.yaml` (generated in Phase 7) is the machine-readable form of
`docs/architecture/03-api-contracts.md` — that document is the source of
truth; this package must not diverge from it. Used to generate the mobile
app's typed REST client + Zod validators.
